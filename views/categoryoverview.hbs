<div id="main">
    <h1>{{title}}</h1>
    <ul id="author_list">
        {{#each category}}
            <a href="/{{path}}{{this.caturl}}">
                {{this.catname}}
            </a>
        {{/each}}
    </ul>
</div>